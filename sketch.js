function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
}
function setup() {
  createCanvas(400, 400);
  console.log("Sketch.js is running!"); // Debugging message
}

function draw() {
  background(220);
}
